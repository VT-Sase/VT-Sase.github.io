/**
 * Events — route: /events
 *
 * Sections to build:
 *   1. Upcoming events list (cards: name, date, location, description)
 *   2. Embedded Google Calendar below it
 */
export default function EventsPage() {
  return (
    <>
      <h1>Events</h1>
      <p>TODO</p>
    </>
  );
}
